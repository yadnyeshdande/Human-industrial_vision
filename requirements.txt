# =============================================================================
# requirements.txt – Industrial Vision Safety System v2.0
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# For GPU support (RTX 3050):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# =============================================================================

# Core computer vision
opencv-python>=4.8.0
numpy>=1.24.0

# YOLO detection
ultralytics>=8.0.0

# GUI
PyQt5>=5.15.0

# Process resource monitoring
psutil>=5.9.0

# Configuration
PyYAML>=6.0

# USB relay hardware (optional – only needed if use_usb_relay: true)
# pyhid-usb-relay>=0.0.2

# GPU monitoring (optional – improves VRAM reporting)
# pynvml>=11.0.0

# PyTorch – install separately with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
